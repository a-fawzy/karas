{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Education list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Start</th>
                <th>End</th>
                <th>Field</th>
                <th>Degree</th>
                <th>Grade</th>
                <th>Description</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('employee_education_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.start }}</td>
                <td>{{ entity.end }}</td>
                <td>{{ entity.field }}</td>
                <td>{{ entity.degree }}</td>
                <td>{{ entity.grade }}</td>
                <td>{{ entity.description }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('employee_education_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('employee_education_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('employee_education_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
